[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200

# Las URLs de los nodos se definen aquí
[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"

# El perfil sepolia solo debe contener datos de red si es necesario, 
# pero rpc_url y private_key no van aquí.
[profile.sepolia]
chain_id = 11155111